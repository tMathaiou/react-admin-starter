# React Admin Starter
React admin starter with minimal configuration.
It has preconfigured authorization system and RESTful API to be used with https://github.com/tMathaiou/node-typescript-skeleton 
```
clone repository
npm install OR yarn
npm start
```
